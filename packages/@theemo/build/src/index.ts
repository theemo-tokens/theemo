/**
 * @packageDocumentation
 *
 * The package for building themes to make them consumable from your frameworks
 * and bundlers.
 *
 * ## Installation
 *
 * ```sh
 * pnpm add -D @theemo/build
 * ```
 */

export { build } from './build';
export type { GenerateConfig } from './config';
